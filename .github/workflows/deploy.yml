name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'
        bundler-cache: true
        
    - name: Install Jekyll
      run: |
        gem install jekyll bundler
        
    - name: Build Jekyll site
      run: |
        jekyll build --destination _site
        
    - name: Check file structure
      run: |
        # Verify required files exist
        if [ ! -f "_config.yml" ]; then
          echo "Error: _config.yml not found"
          exit 1
        fi
        if [ ! -f "_layouts/default.html" ]; then
          echo "Error: _layouts/default.html not found"
          exit 1
        fi
        if [ ! -f "index.html" ]; then
          echo "Error: index.html not found"
          exit 1
        fi
        echo "File structure validation passed"
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: _site
        publish_branch: gh-pages
        force_orphan: true
        
    - name: Deploy status
      run: |
        if [ "${{ github.ref }}" = "refs/heads/main" ] || [ "${{ github.ref }}" = "refs/heads/master" ]; then
          echo "✅ Deployment completed successfully!"
          echo "🌐 Your site will be available at: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"
        else
          echo "ℹ️ This is a pull request - no deployment needed"
        fi 